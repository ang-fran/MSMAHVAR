% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msmvar_mixedlag.R
\name{ms_var_m_step_ml}
\alias{ms_var_m_step_ml}
\title{M-step for the mixed-lag MSM-VAR}
\usage{
ms_var_m_step_ml(
  Y,
  gammaZ,
  smoothed_joint,
  mu_prev,
  A_prev,
  p_vec,
  switching = make_switching(),
  eps = 1e-10
)
}
\arguments{
\item{Y}{Data matrix (\eqn{T \times k}).}

\item{gammaZ}{\eqn{T \times K} smoothed augmented-state posteriors.}

\item{smoothed_joint}{List of smoothed joint transition posteriors.}

\item{mu_prev}{Previous mean list (length \eqn{M}).}

\item{A_prev}{Previous AR list (length \eqn{M}).}

\item{p_vec}{Integer vector of length \eqn{M} (regime lag orders).}

\item{switching}{Named logical list from \code{make_switching()}.}

\item{eps}{Ridge / stability constant.  Default \code{1e-10}.}
}
\value{
Same structure as \code{ms_var_m_step}: a list with
\code{mu}, \code{A_list}, \code{Sigma_list}, \code{P_hat}.
}
\description{
Updates all parameter blocks.  The AR update for regime \eqn{s} builds
a regressor matrix of width \eqn{k \cdot p_s}, which may differ across
regimes.  The weighted normal equations are solved via the Moore-Penrose
pseudoinverse (\code{MASS::ginv}) so that the solve remains well-defined
even when the effective rank is less than \eqn{k \cdot p_s} (e.g. when
regime \eqn{s} has few effective observations or large \eqn{p_s}).
An additional ridge term \code{eps * I} is added before inversion for
numerical stability.
}
