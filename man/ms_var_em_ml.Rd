% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msmvar_mixedlag.R
\name{ms_var_em_ml}
\alias{ms_var_em_ml}
\title{EM estimation for the mixed-lag MSM-VAR}
\usage{
ms_var_em_ml(
  Y,
  p_vec,
  mu_init,
  A_init,
  Sigma_init,
  P_init,
  switching = make_switching(),
  pi = NULL,
  max_iter = 300,
  tol = 1e-06,
  eps = 1e-10,
  step_size = 0.5,
  verbose = TRUE
)
}
\arguments{
\item{Y}{Data matrix (\eqn{T \times k}).}

\item{p_vec}{Integer vector of length \eqn{M} with per-regime lag
orders.  A scalar is broadcast to all regimes (standard equal-lag case).}

\item{mu_init}{List of \eqn{M} initial mean vectors.}

\item{A_init}{List of \eqn{M} initial AR lists.  \code{A_init[[s]]}
must have length \code{p_vec[s]}.}

\item{Sigma_init}{List of \eqn{M} initial covariance matrices.}

\item{P_init}{Initial \eqn{M \times M} transition matrix.}

\item{switching}{Named logical list from \code{make_switching()}.}

\item{pi}{Initial regime probability vector.  Default: uniform.}

\item{max_iter}{Maximum EM iterations.  Default \code{300}.}

\item{tol}{Log-likelihood convergence tolerance.  Default \code{1e-6}.}

\item{eps}{Ridge / stability constant.  Default \code{1e-10}.}

\item{step_size}{EM damping factor in \eqn{(0, 1]}.  Default \code{0.5}.}

\item{verbose}{Print progress every 10 iterations.  Default \code{TRUE}.}
}
\value{
A list with \code{mu}, \code{A_list}, \code{Sigma_list},
\code{P_hat}, \code{loglik}, \code{p_vec}, \code{switching}.
}
\description{
Fits a Markov-Switching Mean-Adjusted Heteroskedastic VAR with
per-regime lag orders \code{p_vec}.  The conditional mean for regime
\eqn{s} is:
\deqn{
  y_t - \mu_s = \sum_{l=1}^{p_s} A_{s,l}(y_{t-l} - \mu_{s_{t-l}})
               + \varepsilon_t, \quad \varepsilon_t \sim N(0, \Sigma_s).
}
Initialization lists \code{A_init[[s]]} must have length \code{p_vec[s]}.
}
\examples{
\dontrun{
# Regime 1 uses 1 lag, Regime 2 uses 2 lags
A_init <- list(
  list(diag(0.3, k)),            # p_1 = 1
  list(diag(0.3, k), diag(0.1, k))  # p_2 = 2
)
fit <- ms_var_em_ml(Y, p_vec = c(1, 2),
                       mu_init, A_init, Sigma_init, P_init)
}

}
